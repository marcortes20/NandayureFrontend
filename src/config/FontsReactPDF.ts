import { Font } from '@react-pdf/renderer';

Font.register({
  family: 'Times-Roman',
  src: '/public/fonts/Times New Roman.ttf',
});

Font.register({
  family: 'Britannic Bold',
  src: '/public/fonts/britannic-bold.ttf',
});
